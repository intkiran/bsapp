{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.1"
  },
  "scripts": {
    "bootstrap": "npm install && npm lerna bootstrap",
    "test": "lerna run test:ci",
    "clean": "lerna clean",
    "setup": "npm install && lerna bootstrap"
  }
}
